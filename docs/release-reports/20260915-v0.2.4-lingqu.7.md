# v0.2.4-lingqu.7 生产发布记录

- 发布状态：已完成
- 发布提交：`9223ca14a`
- 镜像：`local/gavin2api:0.2.4-lingqu.7-9223ca14a`
- 镜像 ID：`sha256:0ed6d9aa35a354143f449b1437ebbb34e311b1ca35dbf861a5227d906021d319`
- 镜像归档：`/opt/gavin2api/gavin2api-0.2.4-lingqu.7-9223ca14a.tar`
- 镜像归档 SHA-256：`85261451b8a7eb277923fc6328a8a012cd96447dd0873ed373361cb5bff16da2`
- 切流时间（UTC）：`2026-09-14T20:13:22Z`

## 本次发布内容

- 管理员分配订阅时支持选择具体套餐档位，并校验套餐与分组的归属关系。
- 兼容生产库已存在的迁移校验格式，避免候选容器因历史 checksum 格式退出。
- 兼容生图订阅组已经完成的名称变更（`GPT Image订阅`、`nano banana订阅`）。
- 订阅目录迁移 243–246 已成功执行；当前 8 个订阅系列共 40 个套餐，40 个套餐的售价货币均为 CNY。

## 发布检查

- 候选容器：`gavin2api-release-0.2.4-lingqu.7-9223ca14a`，状态 `running/healthy`。
- 候选内部 `/health`：通过。
- 候选及公网 `/v1/models` 未授权检查：返回 `401`，路由和鉴权均正常。
- Caddy 配置：`caddy validate` 通过；运行态 upstream 已切换到候选，旧 upstream 已移除。
- API、CDN 公网 `/health`：切换前连续 3 次、切换后连续 3 次均返回 `200`；持久化 Caddyfile reload 后再次返回 `200`。
- Caddyfile 宿主机与容器挂载内容 SHA-256 一致：`81411c72bc840c7785d160f1d7f74a95fc08c935734880d5f195b2dce7f97b1a`。
- 候选启动日志未发现 panic、fatal、数据库连接失败或 Redis 连接失败。

## 依赖与回滚

- 新生产应用容器已设置为 `unless-stopped`，并确认修改重启策略时未重启进程。
- PostgreSQL、Redis、Caddy、SubPilot 的容器 ID、启动时间和重启次数在发布前后保持不变，均未重启。
- 旧应用容器 `gavin2api-release-0.2.4-lingqu.6-a567e8952` 已停止但保留，作为即时回滚点。
- 发布备份目录：`/opt/gavin2api/deploy-backups/20260914T200042Z-gavin2api-release-0.2.4-lingqu.7-9223ca14a`。

按要求未运行完整测试套件；此前已通过针对迁移 checksum 兼容性的定向测试，本次以候选启动、迁移执行、Caddy 运行态和公网连续健康检查作为上线门禁。

## 最终补发：嵌入式前端资源修复

首次候选使用了后端二进制的快速构建路径，后续审计发现其嵌入式前端资源不是最新版本。已重新生成前端生产资源并重新编译、预热和切换，以下为当前最终生产状态：

- 最终构建提交：`675617571`。
- 最终镜像：`local/gavin2api:0.2.4-lingqu.7-675617571`。
- 最终镜像 ID：`sha256:229657d9171daf287185f9d53f8bbada51d53b84b5e5eea769c2262f7757c5e4`。
- 最终镜像归档：`/opt/gavin2api/gavin2api-0.2.4-lingqu.7-675617571.tar`。
- 最终镜像归档 SHA-256：`6fe5ad2a297f55a81ca2fa88251dc51eea68aa70d474080ed95893ae1689b2b3`。
- 最终生产容器：`gavin2api-release-0.2.4-lingqu.7-675617571`，容器 ID `bfe49533cf327f07deaf51e0625056a0d1ca92a7ca7cc1ba09330afe4103d3a2`，状态 `running/healthy`，重启策略 `unless-stopped`。
- 最终切流时间（UTC）：`2026-09-14T21:13:43Z`。
- API 与 CDN 公网实际返回的管理员订阅分包均包含 `plan_id`，确认最新前端已进入公网资源。
- 最终 Caddyfile 宿主机与容器挂载内容 SHA-256 一致：`23745e027df0c85cbd28410f698e61f7fba5ecc601a0209edd54b44a6354ce04`；持久化 reload 后 API/CDN `/health` 仍连续返回 `200`。
- 最终备份目录：`/opt/gavin2api/deploy-backups/20260914T211037Z-gavin2api-release-0.2.4-lingqu.7-675617571`。
- 前一候选 `gavin2api-release-0.2.4-lingqu.7-9223ca14a` 已停止但保留，最终生产版本可即时回滚。
