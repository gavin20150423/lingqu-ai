# v0.2.4-lingqu.6 发布报告

- 发布日期：2026-09-14（Asia/Shanghai）
- 构建提交：`a567e8952226ced13ec7f4e4a691075564697d4b`
- 版本：`0.2.4-lingqu.6`
- 本地镜像：`local/gavin2api:0.2.4-lingqu.6-a567e8952`
- 镜像摘要：`sha256:69c835aabaf7ee03324739cf243e7cf94eb96dfd6fbc8638eb5f01ac2b7472a3`
- 生产新容器：`gavin2api-release-0.2.4-lingqu.6-a567e8952`
- 生产旧容器：`gavin2api-release-0.2.4-lingqu.5-27de55f45`（已停止，保留回滚）

## 发布内容

- 修复结算接口遗漏 `for_sale` 导致正式套餐显示“暂不可用”。
- 正式目录包含 CCMax Claude、Kiro Claude、GPT、Gemini、国模、Grok、GPT Image 和 nano banana，共 8 个系列、40 个按月套餐，全部可售。
- 套餐售价统一按人民币，额度继续按美元单位；生图套餐补充每张额度和档位说明。
- 生图系列名称统一为 `GPT Image订阅` 与 `nano banana订阅`，国模套餐白名单移除 Qwen。

## 验证结果

- 新容器：`running / healthy`，重启次数 0。
- 新容器内部 `/health`：`200`。
- 候选 `/v1/models` 未授权检查：`401`（鉴权正常，非 5xx）。
- Caddy 切换前后公网观察窗口：API `/health` 连续 3 次 `200`，CDN `/health` 连续 3 次 `200`。
- 切换后公网 API/CDN：均 `200`；公网 `/v1/models` 未授权返回 `401`。
- PostgreSQL、Redis、Caddy、SubPilot 未重启，容器启动时间和重启次数保持不变。
- 数据库订阅目录：`40 / 40` 可售，`40 / 40` 为 CNY；8 个正式系列均为 5 档。

## 备份与回滚

- 发布备份：`/opt/gavin2api/deploy-backups/20260913T182204Z-0.2.4-lingqu.6-a567e8952`
- 其中保留 Compose、Caddy、运行时状态、数据库备份和切换前 Caddy 配置。
- 主机 Caddy 配置已持久化为新 upstream，当前运行时配置指向新容器。
- 本次使用本地镜像 + 安全蓝绿切换流程，未使用 GitHub Actions，未执行整栈重启。
